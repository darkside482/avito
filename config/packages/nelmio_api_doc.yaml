nelmio_api_doc:
    documentation:
        info:
            title: Avito test app
            description: This is not bad app
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
